fastapi>=0.115,<0.142
uvicorn[standard]>=0.30
sqlalchemy>=2.0,<3.0
alembic>=1.13
pydantic>=2.9
pydantic-settings>=2.5
psycopg[binary]>=3.2          # D-5: psycopg 3, not psycopg2 (Python 3.14 wheels)
PyYAML>=6.0                   # D-6: seed question bank is YAML
python-dotenv>=1.0
anthropic>=0.40               # Session 2: AI explanation engine
mcp>=1.30.0,<2.0               # Gate C2C-1: Zia MCP client transport (zia_client.py); mcp 2.0
                                # removes streamablehttp_client and this client is not built
                                # against the 2.x API -- upper-bounded until that is verified
httpx>=0.27
slowapi>=0.1.9                 # Gate B1A: rate limiting; tested against 0.1.10 (repo convention is compatible-range, not exact pins -- see every other line in this file)
PyJWT[crypto]>=2.10            # Learner auth: Clerk session JWT (RS256) verification via JWKS; tested against 2.10.1

# dev / test
pytest>=8.3
pytest-asyncio>=0.24
